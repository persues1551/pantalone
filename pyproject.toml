[tool.pantalone]
version = "5.2.0"
distribution = false

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "live: live network probe (skipped when network unavailable)",
]
